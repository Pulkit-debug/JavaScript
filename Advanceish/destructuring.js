// Destructuring the Array

// var array = ["pulkit", 28, "smort"];

// var [name, age, nature] = array;

// console.log(nature);

// Destructuring the Objects

const User = {
  name: "pulkit",
  age: 28,
  nature: "Smort",
};


var {name, age, user} = User;

console.log(age);
